# Edit Database Prompt

When modifying database schemas or fields, always update corresponding UI/web layers to keep data fields aligned. Ensure any added/changed columns are reflected in forms, views, and APIs so the UI stays consistent with the database.